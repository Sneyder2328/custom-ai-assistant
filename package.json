{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "start powershell -Command \"npm run dev:client\" && start powershell -Command \"npm run dev:server\"",
    "dev:client": "cd packages/client && npm run dev",
    "dev:server": "cd packages/server && npm run dev"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.1.5"
  }
}
